const { Sequelize } = require("sequelize");

const sequelize = new Sequelize({
  database: "REDUCTED",
  username: "REDUCTED",
  password: "REDUCTED",
  host: "REDUCTED",
  port: "REDUCTED",
  dialect: "REDUCTED",
});

module.exports = sequelize;
